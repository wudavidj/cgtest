<html>
<head>
	<title>Home</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
  	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
	<link rel="stylesheet" href="../css/homePage.css">
	<link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700" rel="stylesheet">
    <script src="../js/jquery-1.11.0.js"></script>

<script>/*
	$(function(){
		console.log("start");
		$('#addCSS').each(function() {
			var text = $(this).next('.mood-title').text();
			console.log(text);
			if (text == "IN PROGRESS"){
			$('#addCSS').addClass('in-progress');
			}
			else {
			$('#addCSS').addClass('task-complete');
			}
		});
		console.log("end");
	});*/
</script>

</head>
<body style="margin: 0;">

	<div class="icon-bar">
		<div class="navbar-settings">
      <div class="dropdown">
        <button class="dropbtn">&#9776;</button>
        <div id="dropdown-content" style="right:0">
          <a href="/settings">Settings</a>
          <a href="/logOut">Log Out</a>
        </div>
      </div>
    </div>
		<h2>COMMON GROUND</h2>
	</div>

	<div class="main-content-area">
		<div class="new-issue">
      <a href="./createNew">
        <button class="filled-in">Create New Issue</button>
      </a>
		</div>
		<div class="title-text-area">
			<div class="list-title">Task List</div>
		</div>
		<table>
			{{#each objects}}
			{{#if boolean}}
			<tr><td>
				<a href="./issue/{{title}}"><div class="section-title" id="{{title}}">{{title}}</div></a>
					<div class="task-complete">
				<div class="text-area" id="addCSS">
			        	<div class="mood-title">{{status}}</div>
			        				</div>
			        			</div>
  			</td></tr>
  			{{else}}
  			<tr><td>
				<a href="./issue/{{title}}"><div class="section-title" id="{{title}}">{{title}}</div></a>
					<div class="in-progress">
				<div class="text-area" id="addCSS">
			        	<div class="mood-title">{{status}}</div>
			        				</div>
			        			</div>
  			</td></tr>
  			{{/if}}
    		{{/each}}

<!--
			<tr>
    		<td>
					<div class="section-title">Cleaning with Jeff</div>
					<div class="task-complete">
					<div class="in-progress">
			      <div class="text-area">
			        <div class="mood-title">COMPLETED</div>
			    	</div>
					</div>
    		</td>
  		</tr>-->

		</table>
	</div>




</body>
</html>
